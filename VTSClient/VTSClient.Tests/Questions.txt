https://kb.epam.com/display/EMOBCC/XMP+2017+-+2+-+CROSS-PLATFORM

1. REST
Для чего такой зоопарк подходов (HttpClient, WebClient, WebRequest, RestSharp...)?
Какой подход используется на практике?
Какой "правильный" подход для чека конекшена к сервисам?
SOAP вообще имеет место быть?

2. DAL
Запросы "голым" текстом используются? На сколько это правильно?
Или в данном случае с этим ОК, т.к. это локальная БД?
Как обеспечивается синхронизация между локальной БД и сервисом? Каждый раз перебирать все содержимое?

3. IoC
В случае Native Xamarin нет возможности (вроде) использовать DependencyService.
Какой правильный способ рулить зависимостями? Все через "реджистри" (каждая платформа сама конфигурирует себе необходимый набор)?

В случае Xamarin Forms используется дефолтный подход с DependencyService.
Как в этом случае подтягиваются зависимости в какие-нибудь интеграционные тесты?

Есть какой-то универсальный подход, который корректнее использовать в обоих кейсах?

4. Файловые системы
Стоит ли закапываться в файловые системы андройда, ios'а, PCL storage и т.п.?